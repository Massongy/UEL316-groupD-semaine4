{% extends 'base.html.twig' %}

{% block title %}Signaler un commentaire{% endblock %}

{% block body %}
<div class="container mt-5">

    <h1 class="mb-4">Signaler un commentaire</h1>

    <div class="card p-4 shadow-sm">

        <p class="mb-3">
            Vous êtes sur le point de signaler le commentaire suivant :
        </p>

        <div class="alert alert-light border">
            <strong>{{ comment.user.prenom }} {{ comment.user.nom }}</strong><br>
            {{ comment.contenu }}
        </div>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-danger mt-3">Envoyer le signalement</button>
        {{ form_end(form) }}

    </div>

</div>
{% endblock %}
